#Babel Firefox add-on (Jetpack)
This version of the Babel extension is built for the mozilla firefox browser. The content is vertually the same with the exception of how the manifest/index is structured. 
